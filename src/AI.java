public class AI {
    private Player player;
    private int depth;

    public AI() {
        player = /*Settings.AI*/Player.BLACK; // should have Settings from popup window
        depth = /*Settings.DIFFICULTY*/6; // should have Settings from popup window
    }
}
